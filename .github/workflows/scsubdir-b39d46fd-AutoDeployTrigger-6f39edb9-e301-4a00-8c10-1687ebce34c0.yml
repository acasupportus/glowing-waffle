name: Trigger auto deployment for scsubdir-b39d46fd

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-b39d46fd-AutoDeployTrigger-6f39edb9-e301-4a00-8c10-1687ebce34c0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIRB39D46FD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIRB39D46FD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIRB39D46FD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIRB39D46FD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIRB39D46FD_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-b39d46fd
          resourceGroup: cappsint-eu2euap-cni-dedicated
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-b39d46fd:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


