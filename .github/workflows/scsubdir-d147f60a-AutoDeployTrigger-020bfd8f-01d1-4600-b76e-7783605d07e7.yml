name: Trigger auto deployment for scsubdir-d147f60a

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-d147f60a-AutoDeployTrigger-020bfd8f-01d1-4600-b76e-7783605d07e7.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIRD147F60A_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIRD147F60A_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIRD147F60A_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIRD147F60A_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIRD147F60A_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-d147f60a
          resourceGroup: cappsint-eastus-cni-dedicated-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-d147f60a:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


