name: Trigger auto deployment for scsubdir-12b00e9f

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-12b00e9f-AutoDeployTrigger-3fe0c151-6094-471d-9868-61df939d9801.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIR12B00E9F_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIR12B00E9F_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIR12B00E9F_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIR12B00E9F_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIR12B00E9F_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-12b00e9f
          resourceGroup: cappsint-cueuap-cni
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-12b00e9f:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


