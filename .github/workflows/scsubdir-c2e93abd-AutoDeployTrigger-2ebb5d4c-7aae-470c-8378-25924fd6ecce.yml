name: Trigger auto deployment for scsubdir-c2e93abd

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-c2e93abd-AutoDeployTrigger-2ebb5d4c-7aae-470c-8378-25924fd6ecce.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIRC2E93ABD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIRC2E93ABD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIRC2E93ABD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIRC2E93ABD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIRC2E93ABD_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-c2e93abd
          resourceGroup: cappsint-cueuap-cni-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-c2e93abd:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


