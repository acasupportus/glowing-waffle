name: Trigger auto deployment for scsubdir-a8a4e95c

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-a8a4e95c-AutoDeployTrigger-37c38676-8258-46e9-8fd5-1fed618c04bc.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIRA8A4E95C_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIRA8A4E95C_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIRA8A4E95C_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIRA8A4E95C_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIRA8A4E95C_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-a8a4e95c
          resourceGroup: cappsint-cueuap-cni-dedicated
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-a8a4e95c:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


