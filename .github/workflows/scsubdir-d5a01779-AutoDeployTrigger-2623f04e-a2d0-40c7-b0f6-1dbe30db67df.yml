name: Trigger auto deployment for scsubdir-d5a01779

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-d5a01779-AutoDeployTrigger-2623f04e-a2d0-40c7-b0f6-1dbe30db67df.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIRD5A01779_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIRD5A01779_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIRD5A01779_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIRD5A01779_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIRD5A01779_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-d5a01779
          resourceGroup: cappsint-eastus-cni-dedicated
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-d5a01779:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


