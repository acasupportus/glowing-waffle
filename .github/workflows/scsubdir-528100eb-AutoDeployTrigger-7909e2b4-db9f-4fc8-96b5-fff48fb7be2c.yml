name: Trigger auto deployment for scsubdir-528100eb

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-528100eb-AutoDeployTrigger-7909e2b4-db9f-4fc8-96b5-fff48fb7be2c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIR528100EB_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIR528100EB_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIR528100EB_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIR528100EB_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIR528100EB_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-528100eb
          resourceGroup: cappsint-eastasia-cni-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-528100eb:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


