name: Trigger auto deployment for scsubdir-65c3a1aa

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-65c3a1aa-AutoDeployTrigger-12063e42-b5ab-4e00-839a-150a32633ba1.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIR65C3A1AA_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIR65C3A1AA_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIR65C3A1AA_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIR65C3A1AA_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIR65C3A1AA_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-65c3a1aa
          resourceGroup: cappsint-rg
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-65c3a1aa:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


