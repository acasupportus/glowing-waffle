name: Trigger auto deployment for scsubdir-bc6ee65d

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '/docker/**'
    - '.github/workflows/scsubdir-bc6ee65d-AutoDeployTrigger-d47eb167-d635-4807-9cf5-3c4a56989ec5.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SCSUBDIRBC6EE65D_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SCSUBDIRBC6EE65D_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SCSUBDIRBC6EE65D_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          dockerFilePath: /docker/dev.Dockerfile
          registryUrl: cappsinttestregistryprivate.azurecr.io
          registryUsername: ${{ secrets.SCSUBDIRBC6EE65D_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SCSUBDIRBC6EE65D_REGISTRY_PASSWORD }}
          containerAppName: scsubdir-bc6ee65d
          resourceGroup: cappsint-cueuap-cni-dedicated-nonvnet
          imageToBuild: cappsinttestregistryprivate.azurecr.io/scsubdir-bc6ee65d:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


